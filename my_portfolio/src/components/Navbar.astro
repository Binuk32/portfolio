---
// src/components/Navbar.astro
import { Image } from 'astro:assets';
import myLogo from '../assets/logo.png';
---

<header class="sticky top-0 z-50 w-full bg-[#050507]/80 backdrop-blur-md border-b border-zinc-800/50 transition-all duration-200">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 py-4 flex flex-col md:flex-row justify-between items-center gap-4 md:gap-8">

        <div class="flex items-center justify-between w-full md:w-auto">
            <a href="#" class="hover:opacity-80 transition-opacity flex items-center gap-3 group">
                <Image
                        src={myLogo}
                        alt="Binuk Pinsara Logo"
                        class="w-8 h-8 sm:w-10 sm:h-10 object-contain"
                />
                <span class="text-white text-base sm:text-lg font-semibold tracking-tight">
                    BinukPinsara
                </span>
            </a>
        </div>

        <nav class="flex items-center justify-center gap-5 sm:gap-6 md:gap-8 text-xs sm:text-sm font-medium text-zinc-400 overflow-x-auto max-w-full no-scrollbar py-1">
            <a href="#about" class="hover:text-white whitespace-nowrap transition-colors duration-150 py-1">About</a>
            <a href="#experience" class="hover:text-white whitespace-nowrap transition-colors duration-150 py-1">Experience</a>
            <a href="#education" class="hover:text-white whitespace-nowrap transition-colors duration-150 py-1">Education</a>
            <a href="#projects" class="hover:text-white whitespace-nowrap transition-colors duration-150 py-1">Projects</a>
            <a href="#contact" class="hover:text-white whitespace-nowrap transition-colors duration-150 py-1">Contact</a>
        </nav>

    </div>
</header>

<style>
    /* Hide scrollbar utility for the mobile navigation bar */
    .no-scrollbar::-webkit-scrollbar {
        display: none;
    }
    .no-scrollbar {
        -ms-overflow-style: none;
        scrollbar-width: none;
    }
</style>